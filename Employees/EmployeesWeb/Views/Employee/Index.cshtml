@model IEnumerable<Employee>

@{
    ViewData["Title"] = "Index";
}

<partial name="_Notification" />

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="">Employee List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Employee" asp-action="Create" class="btn btn-primary"> <i class="bi bi-plus-circle"></i> &nbsp; Create New Employee </a>
        </div>
    </div>
</div>


<table class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                Employee ID
            </th>
            <th>
                Employee Name
            </th>

            <th>
                Department
            </th>
            <th>
                Salary
            </th>
            <th>
                Action
            </th>
        </tr>
    </thead>

    <tbody>
        @foreach (var obj in Model)
        {
            <tr>
                <td width="10%">
                    @obj.EmployeeID
                </td>
                <td width="40%">
                    @obj.EmployeeName
                </td>
                <td width="25%">
                    @obj.Department
                </td>
                <td width="25%">
                    @obj.Salary
                </td>
                <td>
                    <div class=" btn-group" role="group">
                        <a asp-controller="Employee" asp-action="Edit" asp-route-id="@obj.EmployeeID"
                       class="btn btn-primary mx-2"> <i class="bi bi-pencil-square"></i> Edit</a>
                        <a asp-controller="Employee" asp-action="Delete" asp-route-id="@obj.EmployeeID"
                       class="btn btn-danger mx-2"> <i class="bi bi-trash-fill"></i> Delete</a>
                    </div>
                </td>

            </tr>
        }
    </tbody>
</table>

